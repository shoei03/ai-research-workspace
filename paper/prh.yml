version: 1
rules:
  - expected: Web
    pattern:
      - WEB
      - ウェブ
# ------------------------------------------------
# 句読点の強制置換（ここがキモです）
# ------------------------------------------------
  - expected: ，
    pattern: 、
  - expected: ．
    pattern: 。
